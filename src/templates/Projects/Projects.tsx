import { Box } from "@chakra-ui/react";
import React from "react";

// TODO figure out the error in getProjects
export const Projects = () => {
  return (
    <Box display="flex" flexDirection="column" minH="100%" flex={1} h="100%">
      Projects
    </Box>
  );
};
